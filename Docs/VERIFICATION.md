# 검증 체크리스트 (몰아서 확인용)

> 마지막 갱신: 2026-07-22 / 대상: 세션 5까지 (§8이 이번 세션 몫, §1~5는 이전 세션 미검증분)
> 코드·테스트는 통과 상태(EditMode 92/92, 컴파일 클린). **여기 있는 건 전부 "사람이 눈으로 봐야 하는" 항목**이다.
> 확인하면서 `[ ]` → `[x]`로 바꾸고, 깨진 건 아래 §8 양식으로 적어두면 다음 세션에서 바로 집는다.

---

## 0. 준비 (한 번만)

- [ ] Unity 열고 컴파일 에러 0개 확인
- [ ] `Window ▸ General ▸ Test Runner ▸ EditMode ▸ Run All` → **92/92 초록**
- [ ] `StageSelect.unity` 열고 `Tools ▸ ChainRiposte ▸ Build StageSelect Layout` 실행
      (B2에서 추가된 **LOCK/CLEAR 배지**를 씬에 깔기 위함. 안 해도 잠금 틴트는 동작하지만 배지는 안 보인다)
- [ ] 진행도 초기화: `Tools ▸ ChainRiposte ▸ Progress ▸ Reset Progress`

> 팁: 잠금 때문에 월드2 기믹을 못 볼 땐 `Tools ▸ ChainRiposte ▸ Progress ▸ Unlock All Stages`.
> 현재 상태가 궁금하면 `... ▸ Log Progress`.

---

## 1. 세션 3-4 — 낙하 버그 수정 + 에셋 스왑

### 1-1. 보스 타일이 벽을 우회해 내려오는가 (버그 수정 확인)
**왜**: 벽에 얹힌 타일이 미끄러지는 규칙이 없어서 보스가 공중에 멈춰 있었다. 리필이 기둥 중간을 가로채는 문제도 함께 고쳤다.

- [ ] 벽(W)이 있는 스테이지(**1-1**)로 Main 진입
- [ ] 보스 타일이 스폰될 때까지 플레이 (점수/시간이 쌓이면 확률 상승)
- [ ] **보스 타일이 벽에 막히면 대각선으로 미끄러져 내려오는가**
- [ ] **벽(W)/구멍(X)이 아닌 칸에 빈 칸이 남지 않는가** (한 칸이라도 계속 비어 있으면 버그)

> 의도된 동작: 보스가 벽 위에 얹힌 동안은 대각선이 열릴 때까지 기다린다(그동안 카운트다운 압박 = 기획 의도).
> 전투 돌입 판정은 "열 최하단 활성 셀 도달" 그대로.

### 1-2. 타일 스프라이트가 실제로 표시되는가 (A2 파이프라인)
- [ ] `Assets/_Project/Data/Tile_*.asset` 중 아무거나 골라 `sprite` 슬롯에 이미지 하나 꽂기
- [ ] Main 플레이 → **해당 타일이 색 사각형 대신 그 스프라이트로 나오는가** (스프라이트 지정 시 흰 틴트 = 원색)
- [ ] (선택) `BoardView`의 `wallSprite` / `bossSprite` / `cellSprite` 슬롯도 같은 식으로 동작하는지

---

## 2. B2 — 월드맵 진행도 잠금 + 세이브

### 2-1. 초기 상태
- [ ] `Reset Progress` 후 StageSelect 플레이
- [ ] **1-1만 열려 있고** 1-2~2-3은 어둡게(틴트) + LOCK 배지
- [ ] 잠긴 노드를 클릭하면 **캐릭터가 이동하지 않고** 패널에 `LOCKED / CLEAR THE PREVIOUS STAGE FIRST` + START 비활성

### 2-2. 클리어 → 해금
- [ ] 1-1 START → 보스 잡고 **STAGE CLEAR** → **MAP** 버튼으로 복귀
- [ ] 1-2가 열려 있고 **캐릭터가 1-2에 서 있는가** (가장 앞선 열린 노드에서 시작)
- [ ] 1-1 노드에 CLEAR 배지 + 패널 제목에 `- CLEAR`

### 2-3. 세이브 지속성
- [ ] 플레이 정지 → 다시 플레이 → 해금 상태 유지
- [ ] Unity 에디터를 껐다 켜도 유지 (PlayerPrefs 저장)
- [ ] `Reset Progress` 실행 → 다시 1-1만 열림

> 세이브는 **스테이지 id(에셋 이름) 집합**으로 저장한다. 노드를 씬에서 옮기거나 순서를 바꿔도 안 깨진다.
> 단, `StageDataSO`의 에셋 이름(또는 `stageId` 필드)을 바꾸면 그 스테이지의 기록은 끊긴다.

---

## 3. B3 — 스테이지 기믹 3종 (월드2)

배선: **2-1 = 사슬 / 2-2 = 사슬 + 시한폭탄 / 2-3 = 사슬 + 폭탄 + 전염**, 월드1은 기믹 없음.
수치는 `StageDataSO ▸ Gimmick Tuning`에서 조절 (확률/턴/피해량).

### 3-1. 사슬 결박 (2-1)
- [ ] 회색 띠가 감긴 타일이 **시작부터 3개쯤** 보이는가 (`chainInitialCount` 기본 3)
- [ ] 결박 타일을 **드래그해도 스왑이 안 되는가** (턴도 안 닳아야 함)
- [ ] 위 타일들이 결박 타일 **위에 쌓이고, 그 아래로는 안 내려가는가** (벽과 같은 취급)
- [ ] 결박 타일이 포함된 매치 → **타일은 남고 사슬만 풀리는가** (영혼석도 안 나옴)
- [ ] 사슬이 풀린 뒤에는 정상 낙하/스왑되는가

### 3-2. 시한폭탄 (2-2)
- [ ] 새로 떨어지는 타일 일부에 **빨간 숫자**가 붙는가 (기본 확률 12%, 시작값 3)
- [ ] 턴을 넘길 때마다 숫자가 **1씩 줄어드는가** (스폰된 그 턴은 안 줄어드는 게 정상)
- [ ] 0이 되면 **타일이 사라지고 HP가 깎이는가** (기본 12)
- [ ] 카운트 안에 매치로 없애면 아무 일도 안 일어나는가
- [ ] 폭발로 생긴 빈 자리가 정상적으로 메워지는가 (그 결과 매치가 터지면 영혼석도 들어옴)
- [ ] HP가 0이 되면 **DEFEAT 화면**으로 가는가

### 3-3. 전염 (2-3)
- [ ] 보라색 부패 타일이 **시작 시 2개** 있는가
- [ ] 턴이 지날 때마다 **인접 타일 하나씩 부패로 바뀌는가**
- [ ] 부패 타일은 **스왑도 매치도 안 되는가**
- [ ] 부패 **바로 옆에서 매치를 터뜨리면 부패도 같이 사라지는가**
- [ ] 부패가 보드의 35%쯤에서 **더 이상 안 퍼지는가** (완전 데드락 방지 장치)

### 3-4. 조합 / 안정성 (2-3에서 같이 보기)
- [ ] 사슬 + 폭탄 + 전염이 한 보드에서 동시에 굴러가도 멈추거나 예외가 안 나는가
- [ ] **콘솔에 에러/예외 0개** (특히 `InvalidOperationException: 이미 타일이 있는 셀`류)
- [ ] 둘 곳이 없어서 게임이 진행 불가로 굳는 상황이 생기는가 (생기면 §8 양식으로 재현 절차 기록)

> 특히 봐줬으면 하는 것 — 내가 코드로는 못 잡는 부분:
> ① **폭발 → 낙하 → 새 매치**로 이어지는 연출 순서가 어색하지 않은지
> ② 매치가 통째로 사슬에 막혔을 때(전부 결박) 사슬만 풀리고 턴이 소모되는데, 이게 손해처럼 느껴지는지
> ③ 기믹 3개가 겹친 2-3의 **체감 난이도** — 수치는 인스펙터에서 바로 조절 가능

---

## 4. B4 — 모바일 세로/가로 뷰 대응

**먼저 빌더를 다시 돌려야 한다** (방향 프리셋이 빌더에서 깔린다):
- [ ] `Main.unity` 열고 `Tools ▸ ChainRiposte ▸ Build Main Scene UI`
- [ ] `StageSelect.unity` 열고 `Tools ▸ ChainRiposte ▸ Build StageSelect Layout`

### 4-1. 세로(기존) 배치가 그대로인가
- [ ] Game 뷰를 **1080×1920** 으로 두고 Main 플레이 → HUD/전투/결과가 기존과 동일

### 4-2. 가로에서 성립하는가
- [ ] Game 뷰 해상도를 **1920×1080** 으로 바꾸기 (플레이 중에 바꿔도 즉시 반영돼야 정상)
- [ ] 전투: **PARRY = 왼쪽 사이드 / ATTACK = 오른쪽 사이드**, 보스·게이지가 화면 안에 들어오는가
- [ ] 퍼즐: +ATK/+DEF/+PARRY 버튼이 **오른쪽에 세로로** 쌓이고 보드를 심하게 가리지 않는가
- [ ] 월드맵: 정보 패널이 **오른쪽 컬럼**으로 서고 START가 그 아래에 붙는가
- [ ] 보드/월드맵이 화면에 꽉 차게 다시 맞춰지는가 (카메라 자동 재조정)
- [ ] UI가 통째로 확대/축소돼 보이지 않는가 (기준 해상도 전환)

### 4-3. 배치가 마음에 안 들 때 (여기가 이번 작업의 핵심)
- [ ] 아무 UI 요소나 골라 씬 뷰에서 원하는 위치로 드래그
- [ ] 인스펙터의 `Orientation Layout ▸ **가로(Landscape)로 저장**` 클릭
- [ ] `가로 미리보기` / `세로 미리보기` 버튼으로 두 배치가 번갈아 적용되는가
- [ ] 플레이해서 해당 방향에서 저장한 대로 나오는가

> 코드 수정 없이 배치를 잡는 게 목적이다. 마음에 드는 값이 나올 때까지 드래그 → 저장을 반복하면 된다.
> 컴포넌트가 없는 요소는 `Add Component ▸ Orientation Layout` 으로 붙이면 현재 배치가 양쪽 프리셋에 들어간다.

---

## 5. 데드락 리롤 (둘 수 있는 수가 없을 때 보드 섞기)

빌더 재실행이나 씬 배선은 **필요 없다** — 기존 배너 텍스트를 그대로 쓴다.

**왜 중요한가**: 매치가 없는 스왑은 턴을 소모하지 않으므로, 수가 하나도 없으면 턴이 영영 안 넘어간다.
그동안 보스 카운트다운의 **실시간 축은 계속 흐르므로** 아무것도 못 하는 채로 지는 상태가 된다.

- [ ] 자연 발생을 기다리기 어려우면 **2-3(사슬+폭탄+전염)**에서 판이 조각날 때까지 플레이
- [ ] 수가 없어지는 순간 타일들이 **한꺼번에 새 자리로 날아가는가**
- [ ] 배너에 `NO MOVES — SHUFFLING` 이 잠깐 뜨는가
- [ ] 리롤 뒤에 **턴 수가 그대로인가** (리롤은 턴 미소모)
- [ ] 리롤 직후 **매치가 저절로 터지지 않는가** (공짜 콤보 금지)
- [ ] **벽·보스 타일·사슬 타일·부패 타일은 제자리에 있는가** (보스는 하강 위치가 곧 게임플레이라 절대 안 섞음)
- [ ] 폭탄 타일이 자리를 옮겨도 **숫자가 그대로 따라가는가**

> 규칙상 섞이는 건 '움직일 수 있는 타일'뿐이다. 자유 타일이 극단적으로 적은 판(대부분 사슬·부패)에서는
> 섞어도 답이 안 나올 수 있는데, 그건 스테이지 수치 문제이므로 §8 양식으로 적어주면 튜닝하면 된다.

---

## 6. 아직 안 한 것 (참고)

> 세션 5 몫은 §7 이다.

- 부패/사슬 **전용 스프라이트** — 슬롯(`BoardView`의 `corruptionSprite` / `chainSprite`)은 열려 있고
  지금은 플레이스홀더(보라 사각형 / 회색 띠)로 그린다.
- 결과 화면은 세로/가로 공통 배치(중앙 정렬)로 두었다 — 어색하면 4-3 방식으로 프리셋을 잡으면 된다.

---

## 7. 세션 5 (2026-07-22) — 앱 흐름 · 현지화 · 보스 채보

### 7-0. 먼저 (안 하면 새 UI가 안 보인다)

- [ ] `Main.unity` → `Tools ▸ ChainRiposte ▸ Build Main Scene UI`
- [ ] `StageSelect.unity` → `Build StageSelect Layout`
- [ ] `Tools ▸ ChainRiposte ▸ Build App Scenes (Intro + Title)`

### 7-1. 앱 흐름

- [ ] `Intro.unity`에서 플레이 → 로고 페이드 → 타이틀 → 스테이지선택까지 **페이드로 이어지는지**
- [ ] 인트로가 아무 키/터치로 스킵되는지
- [ ] 타이틀: 세이브가 없으면 **이어하기 비활성**, 새 게임은 확인 후 진행도 삭제
- [ ] 옵션: BGM/SFX 슬라이더 · 화면 방향 고정 · 언어 전환 · 진행도 초기화가 **실제로 먹는지**
- [ ] 화면 방향을 가로 고정하고 게임까지 들어가 유지되는지, 재실행 후에도 남는지

### 7-2. 현지화

- [ ] 전 씬 문구가 **키가 아니라 한글/영문으로** 나오는지 (키가 보이면 `Find Missing Keys In Scene`)
- [ ] 옵션에서 언어를 바꾸면 **게임 전체가** 즉시 바뀌는지 (HUD·전투·결과·월드맵 포함)
- [ ] 한글이 □로 깨지지 않는지, 픽셀 폰트 가독성이 괜찮은지

### 7-3. 퍼즐

- [ ] 벽 밑에 빈 칸이 남지 않는지, 타일이 겹쳐 보이지 않는지
- [ ] 벽이 매치로 깎일 때 **금이 가는 그림으로** 바뀌는지
- [ ] (2-3 등에서) 수가 없어지면 `NO MOVES — SHUFFLING` 후 섞이는지 — **턴은 안 닳아야 한다**

### 7-4. 월드맵

- [ ] 미해금 스테이지가 `BOSS ???` / `HAZARD ???` + **검은 실루엣**인지
- [ ] 들어갔다가 **지고 나와도** 그 스테이지 정보가 공개되는지
- [ ] 잠긴 노드를 누르면 **걸어가다 막혀서 튕기는지**

### 7-5. 보스 전투 (여기가 이번 핵심)

- [ ] 보스 타일 도달 → **준비 화면**이 뜨는지 (예외 없이). 경고 문구 + NPC 자리 2개
- [ ] 준비 화면에서 **HUD의 +ATK/+DEF/+PARRY로 스탯을 찍을 수 있는지**, 시간에 안 쫓기는지
- [ ] FIGHT → 플레이어(왼쪽)와 보스(오른쪽)가 **양쪽에서 미끄러져 등장**하는지
- [ ] **흰 원이 여러 개** 밀려오는지, **플레이어 쪽으로** 모이는지
- [ ] 회색 띠가 눈에 들어오는지 / `+PARRY`를 찍으면 **띠가 굵어지는지**
- [ ] **패링이 손에 맞는지** — 빡빡하면 `PlayerStatsConfig ▸ parryLateGraceSeconds`(0.12)를 올린다
- [ ] Flurry 패턴의 **3연타**에서 원 3개가 줄지어 오고, 패링 3번으로 다 막히는지
- [ ] 공격 버튼 — **빈 박에서만** 나가고, 노트가 올 때 누르면 잠기는지 / 찌르는 모션이 보이는지
- [ ] 준비 시간이 적당한지 (부족하면 채보 에디터에서 노트의 `예비(박)`를 올린다)
- [ ] `Boss_01` 선택 → 인스펙터 **타임라인이 보이고 클릭으로 노트가 찍히는지**

> 튜닝 위치: 준비 시간=노트 `예비(박)` / 원 거리=`CombatScreen ▸ approachSpeed` / 리듬=패턴 `BPM`.
> 셋의 목적이 다르다 — 자세한 건 CLAUDE.md 인수인계 참조.

---

## 8. 세션 6 (2026-07-23) — 캐릭터 선택 + NPC 반응

### 8-0. 먼저

- [ ] `Title.unity` → `Tools ▸ ChainRiposte ▸ Build App Scenes (Intro + Title)` **재실행**
      (캐릭터 선택 패널이 새로 생긴다)
- [ ] `Main.unity` → `Build Main Scene UI` **재실행**
      (NPC 자리에 반응 컴포넌트가 붙고, 대장장이 그림 `blacksmith` 가 기본값으로 깔린다)
- [ ] 대장장이를 `blacksmith2` 로 바꾸고 싶으면 씬의 `BlacksmithNpc/Body` 이미지에서 교체
      (캐릭터와 무관하므로 코드는 성녀만 채운다)

### 8-1. 캐릭터 선택

- [ ] 타이틀 ▸ 새 게임 → (세이브가 있으면 확인 후) **캐릭터 선택 화면**이 뜨는지
- [ ] 카드 2장(기사 / 낭인)이 그림과 이름으로 나오는지, 고른 쪽만 붉게 칠해지는지
- [ ] 설명 문구가 카드에 따라 바뀌는지 / 언어를 바꾸면 이름·설명도 바뀌는지
- [ ] `이 캐릭터로 시작` → 진행도 초기화 + 월드맵으로 넘어가는지
- [ ] `뒤로` → 타이틀로 돌아오고 **진행도는 그대로**인지 (아직 안 지워졌어야 한다)
- [ ] 게임을 껐다 켜도 고른 캐릭터가 유지되는지

### 8-2. 전투 그림

- [ ] 기사로 시작 → 전투에서 왼쪽 플레이어가 `player_knight` 인지
- [ ] 낭인으로 다시 시작 → `player_sekiro` 로 바뀌는지

### 8-3. NPC 반응 (여기가 이번 핵심)

- [ ] 준비 화면 왼쪽 성녀가 **고른 캐릭터에 따라 다른지** (기사=`darksouls_saint` / 낭인=`sekiro_shaman`)
- [ ] **+ATK 또는 +DEF** 를 누르면 **성녀**가 튀어오르며 번쩍이는지
- [ ] **+PARRY** 를 누르면 **대장장이**가 튀어오르며 번쩍이는지 (색이 더 주황빛)
- [ ] 연타해도 자리가 밀리거나 크기가 이상하게 남지 않는지
- [ ] 반응 도중 FIGHT 를 눌러도 다음에 준비 화면이 멀쩡한지

> 튜닝: 반응의 세기는 씬의 `SaintNpc/Body` · `BlacksmithNpc/Body` 에 붙은
> **`NpcReaction`** 인스펙터에서(`punchScale` / `hopHeight` / `duration` / `flashColor`).
> 나중에 스프라이트 시트가 생기면 같은 컴포넌트의 `animator` 슬롯만 채우면 그쪽이 우선한다.

### 8-4. 패링 — 띠(겹침 = 판정) + 즉시 결판

회색이 **얇은 원**이 아니라 **두께 있는 띠**로 바뀌었고, 띠의 범위가 곧 실제 판정 범위다
— 바깥 = 가장 이른 성공, 안쪽 = 가장 늦은 성공(유예 포함).
그리고 **누른 순간 성공/실패가 결판난다** (예전에는 타격 시점까지 기다렸다).

- [ ] 판정 안에서 누르면 **그 자리에서** 「PARRY」가 뜨고 **흰 원이 즉시 사라지는지**
      (원이 끝까지 줄어드는 것을 보고 있지 않아도 되는지)
- [ ] 흰 원이 **띠에 겹쳐 있는 동안** 누르면 되는지 — 특히 **띠 안쪽 끝**에서도
- [ ] 띠보다 **바깥**에서 미리 누르면 실패하고, **잠깐 잠겨서 연타가 안 되는지**
      (헛침 잠금 0.35초 — `PlayerStatsConfig ▸ parryWhiffLockSeconds`)
- [ ] 흰 원이 띠를 **완전히 지나간 뒤** 누르면 실패하는지
- [ ] Flurry 3연타에서 **세 번 따로** 눌러야 하고, 한 번 누를 때마다 원이 하나씩 사라지는지
- [ ] 회색이 너무 진하거나 흐리지 않은지 (`CombatScreen ▸ parryBandColor`, 기본 알파 0.15)

**판정량 하향 (2단계)**

1. 레벨당 증가폭 `0.03 → 0.015`초 (캡 5레벨: 0.40 → 0.325초)
2. **+PARRY 1레벨에 2포인트** (+ATK/+DEF는 1포인트 그대로)
   — 폭을 더 깎으면 성장이 눈에 안 보이므로, 대신 값을 비싸게 매겨 **오르는 속도**를 늦춘다.
   같은 판을 굴렸을 때 예전 5레벨 자리가 이제 2~3레벨쯤이 된다.

- [ ] +PARRY 버튼에 **`2P`**가 표시되는지 / 포인트가 1개일 때 **눌리지 않는지**
- [ ] 포인트 1개일 때 +PARRY가 `MAX`가 아니라 **그냥 비활성**으로 보이는지 (상한과 구분)
- [ ] 캡(5레벨)에 도달하면 그때는 `MAX`로 바뀌는지
- [ ] 한 판 굴려 보고 **판정이 아직도 후한지** — 후하면 `parryPointCost`를 3으로,
      그래도 넓으면 `baseParryWindowSeconds`(0.25)를 손대는 순서로
- [ ] +PARRY를 찍었을 때 **띠가 굵어지는 것이 여전히 보이는지**

> 여전히 안 맞으면 **`approachSpeed`가 아니라 판정 자체**를 의심할 것 —
> 띠는 판정에서 계산되므로 둘이 어긋날 수 없다.
> 손에 안 맞으면 `PlayerStatsConfig ▸ baseParryWindowSeconds`(0.25) /
> `parryLateGraceSeconds`(0.12) / `parryWindowPerLevelSeconds`(0.015) 를 조절한다.
> 실제 아트로 띠를 갈아 끼우면 `CombatScreen ▸ generateBandSprite` 를 꺼야 덮어쓰지 않는다.

### 8-5. 캐릭터 특화

공용 밸런스에 **더해지는** 아주 작은 차이(대략 반 레벨)다.

| | 기사 | 낭인 |
|---|---|---|
| 최대 체력 | +12 | — |
| 피해 감소 | +1.0 (DEF 반 레벨) | — |
| 공격력 | — | +1.5 (ATK 반 레벨) |
| 패링 폭 | — | +0.015초 (PARRY 반 레벨) |

- [ ] 기사로 시작 → 퍼즐 HUD의 **최대 체력이 112** 인지
- [ ] 낭인으로 시작 → 첫 전투의 **패링 띠가 기사보다 아주 살짝 두꺼운지**
- [ ] 콘솔에 `[GameManager] 캐릭터 '...' 특화 적용.` 이 찍히는지
- [ ] 체감상 한쪽이 명백한 정답으로 느껴지지는 않는지 (그렇다면 수치를 더 줄일 것)

> 수치는 `Data/Resources/Characters/Character_*.asset` 인스펙터에서 바로 고친다.
> `Create Default Characters` 를 다시 돌려도 **손으로 고친 값은 덮어쓰지 않는다.**

### 8-6. 스탯 분배를 준비 화면으로 이동

> **`Main.unity` → `Build Main Scene UI` 재실행이 반드시 필요하다.**
> 안 돌리면 퍼즐 화면에 예전 버튼 3개가 **남아 있으면서 아무 반응도 안 한다**
> (PuzzleHud에서 필드가 빠져 배선이 끊긴 채로 남는다).

- [ ] 퍼즐 화면에 **+ATK/+DEF/+PARRY가 없는지** (HP·영혼석·턴·현재 수치 표시는 그대로)
- [ ] 보스 타일 도달 → 준비 화면에서 **FIGHT 아래에 버튼 3개**가 나오는지
- [ ] 거기서 눌러 스탯이 올라가고 라벨·잔여 포인트가 갱신되는지
- [ ] 뒤의 퍼즐판이 **살짝 어두워져** 준비 화면인 게 구별되는지 (판은 비쳐 보여야 한다)
- [ ] 어두운 동안 **퍼즐판을 건드려도 스왑이 안 되는지** (딤이 입력을 막는다)
- [ ] FIGHT를 누르면 어둠이 사라지고 전투로 넘어가는지
- [ ] 가로 화면에서도 버튼 3개와 NPC가 띠 안에 들어오는지

**상점처럼 — 화면 안에서 현황이 다 보이는지**

- [ ] 띠 안에 **체력 / Lv·영혼석·포인트 / 현재 공격·방어·패링**이 나오는지
- [ ] **남은 포인트가 금색으로 크게** 보이는지 (다 쓰면 "준비되면 시작하세요"로 바뀐다)
- [ ] 버튼을 눌러 스탯을 찍으면 **그 줄들이 즉시 갱신되는지** (공격 수치, 남은 포인트)
- [ ] 띠가 반투명이라 뒤 퍼즐판이 **어렴풋이 비치는지**

> 어둠의 세기: `IntermissionScreen ▸ dimColor` (기본 검정 알파 0.45).
> 띠 자체의 투명도는 씬의 `Band` 이미지 알파(0.88).
> UI는 나중에 확장할 자리다 — 지금은 필요한 숫자만 올려 둔 상태.

### 8-7. 월드맵 — 잠긴 노드를 누른 뒤 원래 스테이지로 복귀

**증상**: 1-1에 서서 1-2(잠김)를 누르면 LOCKED가 뜨는데, 1-1을 다시 눌러도
아무 반응이 없어 START를 되살릴 수 없었다. 서 있는 노드를 누르면 무시하고 있었다.

- [ ] 1-1에서 1-2(잠김) 클릭 → 튕기고 `LOCKED` + START 비활성
- [ ] **1-1을 다시 클릭** → 정보 패널이 1-1로 돌아오고 **START가 다시 눌리는지**
- [ ] 그 상태로 START → 1-1이 정상적으로 시작되는지
- [ ] 열린 노드 사이를 오갈 때는 예전처럼 걸어가고 패널이 갱신되는지

### 8-8. 타일 배경판 (아트 대기 중 — 지금은 배선만)

아이콘만 있어서 타일 경계가 안 읽히는 문제. **그림은 사용자가 나중에 넣는다.**
배선은 끝나 있고, 아래처럼 즉시 확인할 수 있다.

**가장 빠른 확인** — 그림 없이 색만으로:

- [ ] `Data/Tile_*.asset` 아무거나 열고 **`Background Color`의 알파를 올린다**
      (예: 어두운 남색 알파 0.5) → 보드에서 그 타일 뒤에 받침이 깔리는지
- [ ] 타일이 **떨어지고 스왑될 때 받침이 같이 따라가는지** (배경 셀과 달리 타일에 붙어 있다)
- [ ] 6종 타일에 서로 다른 색을 넣었을 때 **구별이 확실히 나아지는지**

**그림을 넣을 때**:

- [ ] 타일마다 다르게 → `Tile_*.asset ▸ Background Sprite`
- [ ] 전부 같은 받침 하나로 → `BoardView ▸ Tile Background Sprite` (타일 전용이 우선한다)
- [ ] 받침이 아이콘보다 살짝 커 보이는지 (`BoardView ▸ Tile Background Scale`, 기본 1.05)
- [ ] 벽에는 안 깔리는 게 맞는지 (벽은 셀을 꽉 채우는 지형 — 필요하면 `Background On Walls` 켜기)

> 우선순위: 타일 SO의 전용 그림 → BoardView 공용 그림 → (그림이 없으면) 색만 있는 사각형.
> 알파가 0이면 아무것도 안 그리므로 **아무것도 안 하면 지금과 똑같이 보인다.**

---

## 9. 세션 7 (2026-07-24) — 배경 좌우 왕복 + 캐릭터별 컨셉(테마)

### 9-0. 먼저 (순서대로)

- [ ] `Tools ▸ ChainRiposte ▸ Theme ▸ Create Default Themes`
      → `Data/Resources/Themes/Theme_Irithyll` · `Theme_Ashina` 생성 + 기사/낭인에 자동 연결
- [ ] `Title.unity` 열고 `Tools ▸ ChainRiposte ▸ Theme ▸ Setup Background In Open Scene`
      → 배경 = `ashina` + 좌우 왕복
- [ ] `StageSelect.unity` 열고 같은 메뉴 실행
      → `ThemedBackground` 생성, 색 사각형 `World1Bg`/`World2Bg` 는 꺼짐
- [ ] `Intro.unity` 열고 같은 메뉴 실행 → 배경이 **완전한 검정**이 된다
      (인트로는 로고만 보이는 화면이라 배경 그림도 왕복도 넣지 않는다)

> ⚠ **`Build App Scenes` 는 재실행하지 말 것** — 씬을 새로 만들기 때문에 넣어둔 에셋이 날아간다.
> (빌더에도 같은 배선을 넣어 뒀으므로 나중에 어쩔 수 없이 재빌드할 때는 자동으로 붙는다.)

### 9-1. 배경 좌우 왕복 (`BackgroundPanner`)

- [ ] 타이틀 배경이 **찌그러지지 않고** 원본 비율 그대로 화면을 꽉 채우는지 (좌우가 잘려 나가는 게 정상)
- [ ] 아주 천천히 좌우로 왕복하는지 (한 바퀴 24초 — 양 끝에서 저절로 느려진다)
- [ ] 창을 **가로로 넓혔을 때** 다시 맞춰지는지, 남는 폭이 없어지면 **가운데에서 멈추는지**
- [ ] 세로로 되돌리면 다시 흔들리는지
- [ ] 월드맵 배경이 카메라 화면을 덮는지 / 노드·경로가 배경에 가리지 않는지

> 튜닝: `cycleSeconds`(주기) / `amplitude`(0이면 고정) / `coverScale`(1보다 올리면 비율이 딱 맞는
> 화면에서도 흔들 여유가 생긴다). 월드맵이 산만하면 `amplitude` 0.
> 배경 그림 교체 = 그 Image/SpriteRenderer 의 스프라이트만 갈아 끼우면 된다.

### 9-2. 캐릭터별 컨셉 — 배경

- [ ] **기사**로 새 게임 → 월드맵 배경이 **이루실**인지
- [ ] **낭인**으로 새 게임 → 월드맵 배경이 **아시나**인지
- [ ] 인트로·타이틀은 **캐릭터와 무관하게 그대로**인지 (공용 화면이라 의도된 것)
- [ ] 게임을 껐다 켜도 고른 컨셉이 유지되는지

### 9-3. 캐릭터별 컨셉 — 보스

**겉모습과 이름만 바뀌고 난이도는 공유한다**(HP·체간·채보·패턴은 `BossDataSO` 하나).

- [ ] 기사로 1-1 보스전 → 이름이 **「이루실의 감시자」**
- [ ] 낭인으로 1-1 보스전 → 이름이 **「아시나의 파수꾼」**
- [ ] 두 경우 **보스 HP·체간·패턴이 같은지** (여기가 달라지면 설계가 깨진 것)
- [ ] 언어를 영어로 바꾸면 보스 이름도 영어로 나오는지
- [ ] 보스 **타일**은 컨셉과 무관하게 그대로인지 (일부러 통일 — 보스 타일은 한눈에 알아야 한다)

> 컨셉별 보스 **그림**은 아직 비워 뒀다 — 비어 있으면 `BossDataSO` 의 그림으로 떨어진다.
> 아트가 생기면 `Theme_*.asset ▸ Bosses ▸ Sprite` 에 꽂으면 된다.

### 9-4. 컨셉을 늘릴 때 (코드 수정 없음)

- [ ] `Create ▸ ChainRiposte ▸ Theme` 로 `Data/Resources/Themes/` 에 에셋 추가
- [ ] 배경 키(`map` / `puzzle` / `combat`)에 그림을 꽂고, 보스 항목에 `bossId` + 이름 키
- [ ] `Character_*.asset ▸ Theme` 에서 그 테마를 가리키기 → 끝

> 배경을 스테이지별로 쪼개고 싶으면 키를 `stage.1-1` 처럼 새로 만들고
> 씬의 배경에 붙은 `ThemedSprite ▸ Background Key` 를 그 키로 바꾸면 된다. 코드는 안 바뀐다.

### 9-5. 인트로 — 검정 배경 + 로고 페이드

- [ ] 배경이 **완전한 검정**이고 로고 말고는 아무것도 없는지
- [ ] 로고가 **툭 끊기지 않고** 부드럽게 떠올랐다 잠기는지 (양 끝이 눕는 곡선)
- [ ] 로고 그림이 안 찌그러지는지 (`preserveAspect`) / 너무 크거나 작지 않은지
- [ ] 아무 키나 눌러 스킵되는지

> 타이밍: `IntroController ▸ fadeInSeconds`(0.8) / `holdSeconds`(1.2) / `fadeOutSeconds`(0.6).
> 로고 크기는 씬의 `Canvas/Logo` 를 드래그. 임시 문구 `Caption` 은 지워도 된다.

### 9-6. 월드맵 배경 3층

| 오브젝트 | 테마 키 | 하는 일 | 언제 보이나 |
|---|---|---|---|
| `SkyBackground` | `map` | 배경(하늘·원경) | 세로·가로 **모두** |
| `ThemedBackground` | `path` | **길이 놓인 땅**. 노드를 이 그림 위에 찍는다 | 세로·가로 **모두** |
| `Canvas/TopBackground` | `map` | 길 윗부분을 가려 '창'을 만든다 | **세로만** |
| `BottomBackground` (선택) | `map` | 화면 아래 빈 공간을 채운다 | 세로·가로 **모두** |

- [ ] (BottomBackground 를 넣었다면) 시작 지점 **1-1 아래 빈 곳**에 보이는지
- [ ] 위로 올라가면 **저절로 화면 밖으로 밀려 사라지는지** (world 스프라이트라 스크롤로 가려짐)
- [ ] 길 그림과 **겹치는 위쪽은 길이 덮는지** (sortingOrder −150 이라 빈 아래에서만 보임)
- [ ] 상단 띠·배경과 **같은 그림(map)** 이라 이어져 보이는지 / 캐릭터 바꾸면 같이 바뀌는지

- [ ] `Theme_Irithyll` / `Theme_Ashina` ▸ `Backgrounds` 에 **`path` 슬롯이 생겼는지**
      (지금은 배경과 같은 그림을 재활용한다 — 길 전용 그림이 생기면 여기만 교체)
- [ ] 캐릭터를 바꾸면 `ThemedBackground` 그림도 같이 바뀌는지
- [ ] 길 그림이 **화면을 덮지 않는지** (덮으면 뒤의 배경이 안 보인다 — `BackgroundPanner` 가 꺼져 있어야 정상)
- [ ] 길 그림 크기·위치를 씬에서 드래그해 길에 맞출 수 있는지
- [ ] 세로에서 **상단 띠와 그 아래 배경이 같은 그림이라 이어져 보이는지**
- [ ] 가로에서 배경(`map` 그림)이 **화면 전체**에 그대로 쓰이는지

> 왜 상단 띠와 배경을 나눴나: Overlay 캔버스는 항상 월드 스프라이트 **위**에 그려진다.
> 세로에서 길을 가리려면 UI 여야 하고, 가로에서 길 **뒤**에 깔리려면 월드여야 한다.
> 그래서 같은 `map` 그림을 두 오브젝트가 나눠 맡는다.
>
> ⚠ **월드맵의 두 그림은 화면에 맞춰 늘어나지 않는다**(`BackgroundPanner` 를 꺼 둔다).
> 노드를 그림 위에 찍어서 배치하는데, 실행할 때 그림이 카메라를 따라 움직이면
> 찍어 둔 자리와 그림이 어긋나기 때문이다. 크기·위치는 씬에서 직접 잡는다.
>
> ⚠ **테마별 길 그림은 크기와 길 모양이 같아야 한다.** 노드 위치는 테마와 무관하게
> 하나뿐이라, 이루실과 아시나의 길이 다른 자리에 그려져 있으면 한쪽이 어긋난다.

### 9-6-2. 월드맵 세로 구성 — 위 배경 / 가운데 길 / 아래 정보

**세로에서는 길을 통째로 보여주지 않는다.** 위쪽 배경 띠와 아래쪽 정보 띠 사이의 창에만 담고,
월드2 쪽으로 올라가면 카메라가 따라 올라간다.

- [ ] 플레이했을 때 길과 배경 그림이 **씬 뷰에서 찍어 둔 그대로** 맞물려 있는지
      (어긋나면 그 배경에 `BackgroundPanner` 가 켜져 있는 것 — 꺼야 한다)
- [ ] 길이 **좌우로 벌어진 곳**에서 카메라가 좌우로도 따라가는지
- [ ] 세로(1080×1920)에서 화면이 **위=배경 그림 / 가운데=길 / 아래=정보**로 나뉘는지
- [ ] 처음에는 **1-1 근처만 보이고** 위쪽 길이 배경 띠에 가려 안 보이는지
- [ ] 2-1 쪽으로 이동하면 **카메라가 부드럽게 따라 올라가는지** (뚝 끊기지 않고)
- [ ] 길 끝에서 **더 이상 안 넘어가는지** (빈 공간이 드러나지 않아야 한다)
- [ ] 배경이 **안 움직이는지** (월드맵은 눈이 길을 따라가야 해서 일부러 고정 — 타이틀과 반대)
- [ ] 노드 클릭 판정이 스크롤 뒤에도 맞는지 (엉뚱한 노드가 잡히면 안 된다)

**가로에서는 예전 그대로**

- [ ] 1920×1080 으로 바꾸면 **길 전체가 한 화면에** 들어오는지 (스크롤 없음)
- [ ] 배경이 **화면 전체**를 덮는지 (상단 띠가 아니라)
- [ ] 정보 패널이 **오른쪽 컬럼**이고 길은 왼쪽/가운데인지
- [ ] 세로 ↔ 가로를 몇 번 오가도 배경이 두 개 겹쳐 보이거나 사라지지 않는지

> 튜닝: 확대 정도 = `MapCameraRig ▸ portraitViewWidth`(작을수록 확대·스크롤 길어짐) /
> 따라오는 속도 = `followSmoothTime` / 끝 여백 = `verticalPadding`.
> 띠 높이는 **씬에서 직접** 고친다 — `Canvas/TopBackground` 와 `Canvas/InfoPanel` 의 높이를
> 리그가 매 프레임 재므로 숫자를 두 군데 맞출 필요가 없다.
> 스크롤이 짧게 느껴지면 씬에서 노드를 세로로 더 벌리면 된다(경로선은 따라온다).

### 9-7. 길 그리기 — 씬에서 찍어 노드 배치

`StageSelect` 씬에서 **StageSelectController 를 선택**하면 인스펙터 아래에 「길 그리기」가 생긴다.

- [ ] 씬 뷰에 **노란 곡선**으로 길이 미리 보이는지 (런타임과 같은 곡선)
- [ ] 노드의 **동그란 핸들을 끌면** 경로선이 즉시 따라오는지
- [ ] 「**노드 찍기**」를 켜고 배경 위를 클릭하면 **그 자리에 노드가 생기는지**
      (마지막 노드를 복제하므로 라벨·LOCK/CLEAR 배지가 그대로 따라온다)
- [ ] 새 노드에 스테이지 에셋이 **순서대로 자동 배정**되는지 (1-1 → 2-3, 넘어가면 비워 두고 경고)
- [ ] 「마지막 노드 지우기」로 되돌릴 수 있는지 / Ctrl+Z 로도 되돌아가는지
- [ ] 플레이 → **캐릭터가 그려진 곡선 위를 걷는지** (길에서 벗어나면 안 된다)
- [ ] 노드를 누르면 **카메라가 부드럽게 따라 올라가며** 캐릭터가 그 길을 따라가는지

> 길 모양: `StageSelectController ▸ Path Smoothing` (1이면 예전처럼 꺾은선, 기본 8).
> 경로선과 걷는 길이 **같은 곡선**을 쓰므로 이 값 하나만 바꾸면 둘 다 바뀐다.
> 카메라 속도는 `MapCameraRig ▸ followSmoothTime`.

### 9-7-2. 테마별 길 배치 (씬 하나로)

길 모양이 배경마다 달라도 **씬은 하나**다. 노드 위치를 테마가 데이터로 들고 있다가 런타임에 적용한다.
「길 그리기」 위쪽 **「편집 중인 테마」** 드롭다운에서 오간다.

- [ ] 드롭다운에서 테마를 고르고 **「이 테마에 저장」** → 지금 씬 배치가 그 테마에 들어가는지
      (`Theme_*.asset ▸ Node Layout` 에 좌표가 채워짐)
- [ ] 다른 테마를 골라 길을 다르게 찍고 **저장** → 두 테마가 서로 다른 배치를 갖는지
- [ ] **「이 테마 배치 불러오기」** → 저장한 자리로 노드가 돌아오고 그 테마 배경이 씬 뷰에 뜨는지
- [ ] 기사로 플레이 → **이루실 배치**, 낭인으로 플레이 → **아시나 배치**로 노드가 서는지
- [ ] 한쪽 테마만 저장한 상태로 다른 테마 캐릭터로 플레이하면 **씬 그대로**인지 (경고만, 안 깨짐)

> 노드 **개수**는 모든 테마가 같다(스테이지 수는 공유). 위치만 테마별이다.
> 개수를 바꾸면(노드 추가/삭제) 각 테마에서 다시 저장해야 한다 — 수가 다르면 적용을 건너뛴다.

### 9-8. 타일 배경판을 어디에 꽂는가 (세션 6 배선분 — 위치 안내)

| 하고 싶은 것 | 꽂는 곳 |
|---|---|
| 타일마다 **다른** 받침 그림 | `Data/Tile_*.asset ▸ Background Sprite` (6종: Bat/Ghoul/Potion/Rat/Skeleton/Slime) |
| 타일마다 **다른 색**만 | 같은 에셋 ▸ `Background Color` (**알파를 0보다 올려야 그려진다**) |
| **전부 같은** 받침 하나 | `Main.unity ▸ BoardView ▸ Tile Background Sprite` |
| 색만 공용으로 | `BoardView ▸ Tile Background Color` (알파 0이면 안 그림) |
| 받침 크기 | `BoardView ▸ Tile Background Scale` (기본 1.05) |
| 벽에도 깔기 | `BoardView ▸ Background On Walls` |

- [ ] 우선순위대로 동작하는지: **타일 SO 전용 그림 → BoardView 공용 그림 → 색 사각형 → (알파 0이면) 안 그림**
- [ ] 받침이 타일을 따라 **낙하·스왑에 같이 움직이는지** (고정된 배경 셀과 다르다)

### 9-9. 준비 화면 보스 그림자 (다가오는 연출)

`Build Main Scene UI` 로 `BossShadow` 가 준비 화면 상단에 생겼다(이미 확인함 — "그림자 잘 나옴").

- [ ] 준비 화면 들어가면 위쪽에서 보스 실루엣이 **내려오며 커지고 짙어지는지**
- [ ] 도착 후 제자리에서 천천히 숨쉬는지 / 스탯 찍어도 **다시 안 다가오는지**
- [ ] 기사=이루실 보스, 낭인=아시나 보스 실루엣인지

> 튜닝: `BossShadow` 인스펙터 (`approachSeconds`/`startScale`/`startOffsetY`/`shadowColor`/`idleBobAmplitude`).

### 9-10. 일시정지 / 설정 (전투 씬 우상단)

⚠ **비파괴 메뉴**로 얹는다 — `Build Main Scene UI` 재실행 아님.

- [ ] `Main.unity` 열고 `Tools ▸ ChainRiposte ▸ Add Pause Menu To Main` → `PauseCanvas` 생성
- [ ] `PauseCanvas/TopRight` 의 PauseButton·SettingsButton 이미지에 아이콘 꽂기
- [ ] `PauseMenu` 인스펙터에 `pauseSprite`(⏸)·`playSprite`(▶) 꽂기
- [ ] 퍼즐/전투 중 우상단 ⏸ → **게임이 멈추고**(카운트다운·노트 정지) 아이콘이 ▶로 바뀌는지
- [ ] 다시 누르면 재개 / 딤이 그동안 **퍼즐 입력을 막는지**
- [ ] ⚙ → 옵션 열리며 멈추고, 닫으면 재개되는지
- [ ] 「지도로 나가기」 → 확인 후 월드맵으로 (timeScale 원복되어 다음 씬 안 멈추는지)

### 9-11. 결과 화면 — 클리어/패배 분리

- [ ] **보스 인살(승리)** → 다시 시작 버튼 없이 **잠깐 뒤 지도로 자동 복귀**하는지 (기본 1.6초)
- [ ] **패배**(전투/퍼즐 턴소진) → **다시 시작 / 지도** 두 버튼이 뜨는지
- [ ] 일시정지 중 죽거나 나가도 다음 씬이 안 멈추는지 (timeScale 원복)

> 승리 텀 = `ResultScreen ▸ victoryToMapDelay`. 나중에 인살 컷씬이 이 자리를 채운다.

### 9-12. 버튼 눌림 스프라이트 (사용자 직접 배선 중)

- [ ] 빌더가 만든 버튼이 이제 **누를 때 색이 살짝 변하는지**(ColorTint 기본)
- [ ] 아트 버튼: `Button ▸ Transition = Sprite Swap` + `Pressed Sprite` 꽂으면 눌림 그림으로 바뀌는지

---

## 10. 세션 8 (2026-07-27) — 버튼 눌림 그림 + 테마 보스·배경

### 10-0. 먼저 (순서대로)

- [ ] `Main.unity` 를 연 채로 `Tools ▸ ChainRiposte ▸ UI ▸ Apply Pressed Sprites (Open Scene)`
      → 콘솔에 「배선 N개 / 건너뜀 M개」와 버튼별 목록이 뜬다
- [ ] `Tools ▸ ChainRiposte ▸ Theme ▸ Create Default Themes`
      → 비어 있던 보스 그림·퍼즐/전투 배경 슬롯이 채워진다 (**손으로 꽂아 둔 것은 안 건드린다**)
- [ ] `Main.unity` 를 연 채로 `Tools ▸ ChainRiposte ▸ Theme ▸ Setup Background In Open Scene`
      → `PuzzleBackground`(월드) 생성 + `CombatScreen ▸ Root/Background`(UI) 생성

> 세 메뉴 모두 **비파괴**다 — 오브젝트를 지우고 다시 만들지 않으므로 손으로 꽂은 UI 가 안 날아간다.
> ⚠ 그래도 `Build Main Scene UI` / `Build App Scenes` 는 여전히 재실행 금지.

### 10-1. 버튼 눌림 그림 (Sprite Swap)

규칙은 이름 하나다 — 버튼 그림이 `btn_wide` 면 **같은 시트 안의** `btn_wide_pressed` 를 찾아 꽂는다.
짝이 없는 버튼은 **건드리지 않는다**(색 틴트 그대로).

- [ ] `btn_wide` 를 쓰는 버튼을 누르고 있는 동안 **눌린 그림**으로 바뀌는지
- [ ] 손을 떼면 원래 그림으로 돌아오는지 (하이라이트가 남지 않는지)
- [ ] 우상단 **일시정지 버튼**: ⏸ 일 때와 ▶ 일 때 **각각 다른 눌림 그림**이 뜨는지
- [ ] 짝이 없어 건너뛴 버튼(콘솔 목록)이 여전히 **색 틴트로 눌림 반응**을 하는지
- [ ] 누를 때 버튼이 **사라지는 곳이 없는지** (빈 Pressed 슬롯이 있으면 그렇게 된다)

> 눌림 조각을 새로 슬라이스했으면 메뉴를 **다시 실행**하면 붙는다. 이미 배선된 버튼은 그대로 둔다.

### 10-2. 캐릭터별 보스 겉모습 (기획 자리)

**어떤 보스가 나오는지는 기획이 정한다** — 툴은 그림을 고르지 않는다.

평소 채우는 곳은 **`Boss_*.asset ▸ 캐릭터별 겉모습`**: `캐릭터 + 그림 + 이름 키` 한 줄.
보스 2개 × 캐릭터 2명 = **4줄**이면 끝이고, 그 보스를 쓰는 스테이지가 전부 따라온다.

우선순위: **① 보스 에셋의 캐릭터별 줄 → ② 보스 에셋의 공용 그림·이름**. 자리는 이 하나뿐이다.

- [ ] 아무 그림도 안 넣은 지금 상태에서 **예전 그대로** 나오는지 (1-1 = `Boss_01` 의 버섯, 두 캐릭터 모두)
- [ ] 이름은 캐릭터별로 갈리는지 — 기사 「이루실의 감시자」 / 낭인 「아시나의 파수꾼」
      (이름 키는 테마에서 보스 에셋으로 옮겨 뒀다. 안 갈리면 마이그레이션이 빠진 것)
- [ ] `Boss_01 ▸ 캐릭터별 겉모습` 에 기사 → 그림 A, 낭인 → 그림 B 를 넣으면 갈리는지
- [ ] **준비 화면의 다가오는 그림자가 그 보스와 같은 그림인지** (전투와 그림자는 같은 규칙을 부른다)
- [ ] **HP·체간·채보·패턴은 두 캐릭터가 같은지** (여기가 달라지면 설계가 깨진 것)
- [ ] 한쪽만 채우고 다른 캐릭터를 비워 두면, 비운 쪽만 공용 그림으로 떨어지는지
- [ ] 언어를 영어로 바꿔도 이름이 따라 바뀌는지

> 스테이지에는 **어떤 보스인지(`Boss Data`)만** 적는다. 겉모습을 스테이지에 적으면 같은 그림을
> 스테이지 수만큼 되풀이하게 되고 한 곳만 빠뜨려도 보스가 달라지므로, 그 칸은 두지 않았다.
> `Boss_02` 는 공용 그림이 비어 있다 — 채우기 전까지 전투에서 보스가 안 보인다(고를 그림은 기획).
> 테마(`Theme_*.asset`)는 이제 **배경 + 노드 배치**만 맡는다. 보스 목록은 없앴다.

### 10-3. 퍼즐 / 전투 배경 — 지금은 안 쓴다

전용 아트가 없어 **자리를 걷어냈다.** 테마의 `puzzle`·`combat` 키는 빈 칸으로만 남아 있다.

- [ ] `Main.unity` 를 연 채로 `Tools ▸ ChainRiposte ▸ Theme ▸ Remove Screen Backgrounds From Main`
      → `PuzzleBackground` 와 `CombatScreen ▸ Root/Background` 가 사라졌는지
- [ ] 퍼즐·전투 화면이 **예전 모습 그대로**인지

> 나중에 배경 아트가 생기면: 테마의 `puzzle`·`combat` 칸에 그림을 꽂고
> `Setup Background In Open Scene`(Main 씬) 을 실행하면 자리가 다시 깔린다.

### 10-4. 패링 회색 띠 — 보이는 겹침 = 판정

**고친 것**: 흰 원은 두께가 있는 그림인데 판정은 그 원의 **바깥 테두리**로 계산하고 있었다.
그래서 "띠에 걸친 것처럼 보이는데 아직 판정 밖"인 구간이 원 두께만큼 통째로 있었다.
이제 **원의 안쪽 테두리가 노트의 위치**다 — 안쪽 테두리가 띠에 들어오면 판정 시작, 띠의 안쪽 끝을 지나면 유예 종료.

- [ ] 흰 원이 회색 띠에 **닿는 순간** 눌러서 패링이 되는지 (예전엔 조금 더 기다려야 했다)
- [ ] 띠 안에 겹쳐 있는 **아무 때나** 눌러도 되는지 — 겹쳐 있는데 실패하는 순간이 있으면 안 된다
- [ ] 띠를 완전히 지나간 뒤에 누르면 **실패**하는지 (겹치지 않는데 성공해도 안 된다)
- [ ] 공격 직후(0.4초 커밋) 패링이 **씹히는 건 그대로** — 쿨다운 후 누르면 정상인지
      (이건 의도된 동작이다. 그래도 밀린 느낌이 남으면 커밋 0.4초를 줄이는 게 다음 손잡이)

> 실제 원 아트로 갈아 끼우면 **`CombatScreen ▸ 노트 원 그림의 안쪽 구멍 비율`** 을 그 그림 값으로 고쳐야
> 보이는 것과 판정이 계속 맞는다 (기본 원은 0.88).

### 10-5. 패링 판정 폭 — 무투자는 어렵게, 성장은 눈에 보이게

`PlayerStatsConfig` 변경: `baseParryWindowSeconds` 0.25 → **0.13**, `parryWindowPerLevelSeconds` 0.015 → **0.024**.
유예(0.12)와 캡(5레벨)은 그대로. 결과: **캡에서의 폭이 예전의 기본값과 같아진다.**

| | 윈도우 | +유예 = 총 판정 | 띠 두께(scale) |
|---|---|---|---|
| 예전 Lv0 | 0.250 | 0.370 | 0.333 |
| 예전 Lv5 | 0.325 | 0.445 | 0.400 |
| **지금 Lv0** | **0.130** | **0.250** | **0.225** |
| **지금 Lv5** | **0.250** | **0.370** | **0.333** |

- [ ] PARRY를 하나도 안 찍은 첫 보스전이 **실제로 빡센지** (여기가 좁은 게 정상이다)
- [ ] 준비 화면에서 +PARRY를 찍을 때마다 **띠가 눈에 띄게 굵어지는지** (예전엔 +20%라 안 보였다)
- [ ] 캡(5레벨)까지 찍었을 때가 **예전 기본 느낌**과 비슷한지
- [ ] HUD의 현재 판정 수치(`puzzle.stats`)가 같이 바뀌는지

> 더 조일 순서: `parryWindowPerLevelSeconds`(캡이 너무 관대할 때) → `baseParryWindowSeconds`(초반이 너무 관대할 때)
> → `parryLateGraceSeconds` 0.12(늦은 입력 관용이 과할 때).

### 10-6. 보스가 왼쪽을 본다

그림이 전부 오른쪽을 보고 그려져 있어서, **오른쪽에 서는 보스만** 좌우로 뒤집는다.

- [ ] 전투에서 보스와 플레이어가 **서로 마주 보는지**
- [ ] 패링/피격으로 보스가 튈 때(펀치 연출) **뒤집힌 채로** 튀는지 (한 프레임 원래대로 돌아가면 안 된다)
- [ ] 왼쪽을 보고 그린 보스 그림을 쓸 땐 `CombatScreen ▸ 보스를 좌우로 뒤집기`를 끄면 되는지

> 준비 화면의 **다가오는 그림자**(`BossShadow`)는 건드리지 않았다 — 위에서 내려오는 실루엣이라
> 마주 볼 상대가 없다. 뒤집어야 할 것 같으면 알려줄 것.

### 10-7. 월드맵 — 길을 눌러도 움직인다

세로 스크롤에서 **다음 노드가 화면 밖이라 누를 수가 없던** 문제.
이제 노드를 못 맞히면 **길**을 맞혔는지 보고, 맞혔으면 **누른 쪽으로 한 칸** 움직인다.

- [ ] 다음 노드가 화면 밖일 때 **길 위를 눌러** 앞으로 한 칸 가는지
- [ ] 뒤쪽 길을 누르면 **뒤로** 한 칸 가는지 (방향이 앞으로 고정돼 있으면 안 된다)
- [ ] 길에서 먼 빈 곳을 누르면 **아무 일도 없는지**
- [ ] 서 있는 자리 근처 길을 누르면 정보 패널이 다시 뜨는지
- [ ] 한 칸 앞이 잠긴 노드면 예전처럼 **부딪혀 튕기고 LOCKED** 가 뜨는지
- [ ] 노드를 직접 누르는 예전 방식이 그대로 되는지 (여러 칸 이동 포함)

> 판정 반경은 `StageSelectController ▸ 클릭을 '길'로 인정하는 반경`(기본 1.2 월드 유닛).
> 너무 잘 잡히면 줄이고, 0으로 두면 이 기능이 꺼진다.
> 한 번에 한 칸씩만 가는 게 답답하면 말할 것 — 누른 지점의 노드까지 한 번에 가게 바꿀 수 있다.

---

## 11. 문제 보고 양식

깨진 게 있으면 이 형식으로 적어두면 다음 세션에서 바로 집는다. (어느 절이든 이 양식으로)

```
[항목] 3-2 시한폭탄
[스테이지] 2-2
[증상] 숫자가 0이 됐는데 타일이 안 사라짐
[재현] 폭탄 2개가 같은 열에 있을 때 아래쪽만 터짐
[콘솔] (에러 메시지 있으면 첫 줄만)
```

정보가 애매하면 **콘솔 로그 전문 + 어느 스테이지 + 몇 번째 턴**만 있어도 충분하다.
