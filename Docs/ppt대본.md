# ChainRiposte PPT — 재작성 대본 (14장)

기존 26장 버전은 **중복 슬라이드가 많고 한 장에 담은 양이 너무 많았다.**
이 문서는 그것을 대체하는 **최종 대본**이다. 아래 텍스트를 그대로 슬라이드로 옮기면 된다.

---

## 제작 규칙 (반드시 지킬 것)

**형식**

1. **총 14장.** 「챕터 N」 구분 슬라이드는 **만들지 않는다** — 그것만으로 10장이 늘어난다.
2. 슬라이드 한 장 = **블록 3개 이하**, 블록 하나 = **2줄 이내**. 넘치면 문장을 줄이지 말고 **덜어낸다**.
3. 표는 **4행 이하**. 그보다 길어지면 표가 아니라 문단으로.
4. 같은 내용을 두 장에 넣지 않는다. (기존 버전에서 「런타임 안정성」·「Future Work」·
   「보스 타일 도배」·「설계 원칙」이 각각 **두 번씩** 나왔다.)
5. 아래 대본에 **없는 문장을 지어내지 않는다.** 수치는 전부 실측값이라 바꾸면 틀린다.

**내용 — 쓰지 말 것**

- ❌ **툴을 지웠다 / 삭제했다 / 절반을 덜어냈다 / 정리 전에는 몇 줄이었다** — 이런 서술 일체 금지.
- ❌ `Build Main Scene UI` · `Build App Scenes` · `EditorUiFactory` 같은 **없어진 것의 이름**.
- ❌ 「에디터가 한때 30%였다 → 19%로 줄었다」 식의 **전/후 비교**.
- ✅ 에디터 이야기는 **지금 있는 툴 34종이 무엇을 하고 왜 계속 유지보수되는가**만 다룬다.

---

# 1장 — 표지

> **ChainRiposte**
> 고쳐도 안 무너지는 구조

Unity 6 · C# · 1인 개발 · 3주 (2026.07.14 ~ 08.03)
170파일 / 25,546줄 · 157커밋 · EditMode 테스트 167개 전부 통과

---

# 2장 — 게임: 두 장르를 한 자원으로 묶었다

> **부제**: 퍼즐은 준비 구간, 보스전은 실력 구간.

**한 판의 흐름**
월드맵 → 퍼즐(매치3) → 준비(스탯 분배) → 보스전(패링) → 다음 고리
죽으면 **사슬만 끊기고 성장은 남는다.**

**두 장르가 이어지는 지점**
퍼즐에서 캔 소울로 레벨업하고, **퍼즐에서 맞은 HP가 그대로 보스전으로 넘어간다.**

**핵심 설계 원칙 (이 한 줄이 모든 밸런스의 기준)**
> 난이도는 **실행(패링)**에서 오고, 성장은 **모서리만 깎는다.**
성장이 난이도를 지우면 퍼즐이 무의미해지고, 아무것도 안 하면 성장이 무의미해진다.

*(스크린샷: 퍼즐 화면 + 보스전 화면 나란히)*

---

# 3장 — 의존 방향을 컴파일러가 강제한다

> **부제**: `noEngineReferences: true` 한 줄이 「약속」을 「빌드 에러」로 만들었다.

**어셈블리 4계층 (위에서 아래로만 참조)**

| 계층 | 규모 | 무엇 |
|---|---|---|
| **Editor** | 19파일 / 4,920줄 | 채보·보드·현지화 툴 (커스텀 메뉴 34종) |
| **Game** (어댑터) | 74파일 / 12,464줄 | MonoBehaviour · UI · VFX · Audio |
| **Core** (순수 C#) | 59파일 / 5,025줄 | ★ **UnityEngine 참조 불가 — 컴파일 강제** ★ |
| **Core.Tests** | 18파일 / 3,137줄 | EditMode 167개 · 에디터 없이 실행 |

**왜 규칙을 코드로 강제했나**
"Core에 Unity를 쓰지 말자"는 **지켜지지 않는다.** 그것이 컴파일 에러가 된 순간부터 규칙이 진짜가 됐다.
→ 이 결정이 실제로 값을 한 순간은 **10장**에 있다.

---

# 4장 — 경계를 넘는 방법이 3가지뿐이다

> **부제**: 배선이 전부 같은 패턴을 따르면, 새 기능도 그 패턴에 붙는다.

**규칙 3가지**
1. **밸런스 수치는 전부 ScriptableObject** — 코드에 상수로 박힌 밸런스가 **0**이다 (SO 10종).
2. **Unity 타입은 `Func<>`로 감싼다** — `AnimationCurve` → `Func<float,float>`.
   Core는 커브를 "값을 돌려주는 함수"로만 알므로 Unity를 몰라도 된다.
3. **Core는 C# event 25종으로만 말한다** — 역방향 참조 없이 화면이 반응한다.

**코드에 고유명사를 적지 않는다**
`if (stageName == "2-1")` 같은 코드가 없다. 스테이지별 난이도도 표로 적지 않고
**고리 깊이 × 증가율로 계산**한다 — 표였다면 스테이지를 늘릴 때 갱신을 잊어 **새 판만 조용히 물렁해진다.**

**확장 비용**
스테이지 추가 = 에셋 1개 · 캐릭터 추가 = 에셋 1개 · 기믹 추가 = `StageGimmick` 상속 1개 · 언어 추가 = CSV에 열 1개

---

# 5장 — 어필 기술 ① 리듬 패링 전투

> **부제**: 프레임레이트와 무관하게, 같은 입력이면 같은 결과가 나온다.

**채보(악보) 기반**
보스 공격을 **박(beat) 단위 노트**로 찍는다 — 타격 시점 · 예비동작 길이 · 피해 배율.
**연속기는 별도 타입이 아니다** — 노트를 촘촘히 찍으면 그게 연속기다.

**보이는 것이 곧 판정**
다가오는 흰 원의 **안쪽 테두리**가 노트의 위치이고, 회색 띠의 두께가 **판정 폭 그 자체**다.
PARRY 스탯을 올리면 띠가 **눈에 띄게 굵어진다** — 로직과 뷰가 같은 수치에서 파생되므로 어긋날 수 없다.

**결정적 시뮬레이션**
프레임 단위로 흘리지 않고 **다음 상태 경계**(타격 시점·윈도우 만료)까지만 진행한다.
→ 엔진 없이 순수 C#으로 **전투 전체를 테스트**할 수 있다.

*(영상 5초: 패링 판정 링 — 정지 이미지로는 설명이 안 된다)*

---

# 6장 — 어필 기술 ② 퍼즐 엔진

> **부제**: 모델은 완결된 결과를 한 번에 내고, 뷰는 그 기록을 재생만 한다.

**모델 / 뷰 분리**
```
PuzzleEngine.Swap(a, b) → SwapResult (캐스케이드·낙하·기믹이 전부 담긴 완결 기록)
                                  ↓
                       BoardView가 코루틴으로 재생
```
연출이 도는 **중에도 모델은 이미 최종 상태**다. → 연출을 통째로 스킵해도 게임 상태가 동일하다.

**비정형 보드를 버티는 규칙들**
하트·해골 모양 보드 · 중력 + 대각 슬라이드 · **데드락 검출 후 턴 미소모 리롤**
(매치 없는 스왑은 턴을 안 먹으므로, 수가 없으면 게임이 그냥 멈춘다)

**핵심 불변식**
> 화면에 보이는 빈칸은 **의도한 구멍뿐**이다.
갇힌 칸은 옆 타일이 미끄러져 들어와 메운다 — **실측 99.08%.**

*(영상 5초: 낙하·연쇄)*

---

# 7장 — 어필 기술 ③ 에디터 툴 34종

> **부제**: 기획·아트 반복 작업을 코드로 지웠다. 전체 코드의 19%가 툴이다.

| 툴 | 해결한 것 |
|---|---|
| **채보 에디터** | 타임라인 클릭/드래그 · 스냅(1·1/2·1/4박) · 예비동작 시각화 · **▶ 미리듣기** |
| **보드 그리드 에디터** | 문자열 대신 **칠하는 격자**. 인스펙터 + 전용 창 |
| **현지화 툴** | 구글 시트 → CSV 동기화 · **씬 내 누락 키 검사** |
| **지도 노드 편집** | 씬 뷰 핸들 드래그 · 테마별 레이아웃 저장/불러오기 |

**툴도 데이터 주도다**
눌림 스프라이트 배선은 목록을 코드에 적지 않고 **이름 규칙**(`btn_wide` → `btn_wide_pressed`)으로 찾는다.
→ **아트가 늘어도 툴은 안 고친다.**

**에셋 생성 툴은 빈 슬롯만 채운다**
다시 눌러도 손으로 고쳐 둔 값이 안 지워진다. **사람은 반드시 두 번 누른다.**

*(스크린샷: 채보 에디터 타임라인)*

---

# 8장 — 툴을 만들며 세운 규칙

> **부제**: 계속 쓸 도구여야 하므로, 툴에도 설계가 필요했다.

**① 미리듣기가 아낀 시간**
없을 때: 인스펙터에서 찍기 → 플레이 → 퍼즐 클리어 → 보스전 도달 → 확인 **(2~3분)**
있을 때: 찍기 → **▶ 클릭 (2초)**. 채보 튜닝은 수십 번 반복하는 일이라 여기서 시간이 가장 많이 샌다.

**② 그리는 코드는 한 벌, 편집 상태는 호스트마다**
보드 편집은 **인스펙터**와 **전용 창** 두 곳에서 한다(인스펙터는 폭이 좁아 20×20이 잘린다).
그리는 코드를 각자 들면 브러시 하나 늘릴 때 두 곳을 고쳐야 하고 **한쪽은 반드시 잊는다** → `BoardGridGUI` 한 벌.
반대로 편집 상태를 static으로 두면 **둘을 같이 열었을 때 서로 덮어써 드래그가 끊긴다** → 호스트마다 하나씩.

> 공유할 것(**그리는 방법**)과 나눌 것(**지금 누가 끌고 있는가**)을 가르는 것이 툴 코드의 절반이다.

**③ 두 번 돌려도 무해해야 한다**
일괄 교체 툴이 두 번째 실행에서 껍데기를 한 겹 더 씌운 버그를 겪고,
**조상 노드 제외 + 이미 처리된 것 건너뛰기** 2중 방어를 넣었다.

---

# 9장 — 어필 기술 ④ 현지화

> **부제**: 원천을 한 곳으로 묶으면, 성능 최적화까지 정확해진다.

**구조**
원천은 **구글 시트 → CSV 한 장**(139키 × 2언어). 코드·씬에 문자열 하드코딩 **0**.
정적 문구는 `LocalizedText` 바인더 + 키, 동적 문구(HP·턴)는 `Loc.GetText(key, args)`.

**함정 하나를 규칙으로 굳혔다**
동적 문구에 바인더를 붙이면 언어 전환 시 **코드와 컴포넌트가 서로 덮어쓴다.**
같은 칸을 여러 문구가 돌려 쓸 때는 **값이 아니라 키를 갈아 끼운다**(`SetKey`).

**그래서 구울 글자를 정확히 알 수 있다**
화면 글씨의 원천이 CSV 한 장뿐이라 **거기 없는 글자는 게임에도 안 나온다.**
실측 **343자**(한글 274)를 시작 로딩에서 한 번에 구워 런타임 끊김을 없앴다.

---

# 10장 — 아키텍처 결정이 3주 뒤에 값을 한 순간

> **부제**: Core에 UnityEngine이 없어서, 에디터를 켜지 않고 보드 60,000개를 검증할 수 있었다.
> ⭐ **발표에서 가장 힘을 줄 슬라이드.**

**상황**
코드 주석은 *"벽과 구멍 이외의 모든 칸이 항상 채워진다"*고 **주장**했고,
비정형 보드 퍼즈를 포함한 **테스트 166개가 통과 중이었다.** 그런데 실기에서 벽 밑에 빈 칸이 남았다.

**방법**
`.csproj`에 `<Compile Include=".../Core/**/*.cs" />` **한 줄** → Unity 없이 콘솔 앱으로 컴파일.
무작위 보드 **60,000개**를 훑어 반례를 수집했다. 같은 방식으로 테스트도 **`dotnet test` 0.3초** 완주.

**결과**
이번에 나온 버그 **3개를 전부 퍼즈가 잡았다.** 손으로 짠 테스트 166개는 **하나도 못 잡았다.**

> **불변식은 주장이지 보장이 아니다.** 테스트가 있다는 사실이 오히려 안심시켰다.
> `noEngineReferences` 한 줄이 없었다면 이 반례를 찾을 방법 자체가 없었다.

---

# 11장 — 트러블슈팅 대표 3건

> **부제**: 재발하는 버그는 주의가 아니라 코드로 막는다.

**① 게이지가 안 줄어든다 — 3번 재발**
`Image.type`이 `Filled`가 아니면 `fillAmount`가 **아무 일도 안 한다.**
UI 스프라이트를 꽂을 때 Unity가 9슬라이스를 감지해 `Sliced`로 되돌려 놓고 있었다.
→ `Awake`에서 게이지 3종을 강제. **세 번 재발했다는 것은 코드 레벨 방어가 필요했다는 뜻이다.**

**② 벽 밑에 빈 칸 — 불변식이 거짓이었다**
대각 슬라이드는 **한 칸짜리 이동**이라 출처가 정확히 대각선 위여야 하는데,
그 자리가 벽·보드 밖·구멍이면 **도달 가능한 타일이 물리적으로 없다**(가장 흔한 경우는 *부서진 벽이 남긴 자리*).
→ 구멍을 리필 입구까지 **걸어 올라가게** 해 옆 타일로 메운다. **뷰는 한 줄도 안 고쳤다.**

**③ 보스 타일이 보드를 도배한다 — 확률의 단위**
확률이 **리필 타일 하나하나에** 굴려지고 있었다. 한 웨이브 10칸이면 주사위를 10번 던진다.
**"5%"가 실제로는 `1 − 0.95¹⁰ = 40%`였다.** → 동시 상한 + 웨이브 누적 카운터.
**확률은 "무엇 당(per)"인지 같이 적는다.**

---

# 12장 — 최적화 & 런타임 안정성

> **부제**: 설정 하나가 게임의 핵심 손맛을 갉아먹고 있었다.

**🔴 30fps 상한 — 성능만의 문제가 아니었다**
아무도 `Application.targetFrameRate`를 안 정했다. 기본값 −1 = 플랫폼 기본값이고 **모바일에서 그건 30**이다.
한 프레임 33ms는 **Lv0 패링 판정 폭(0.25초)의 13%** — 정확히 눌러도 프레임 경계에 걸려 놓친다. → **60 고정.**

**자산 최적화**

| 항목 | 조치 | 이유 |
|---|---|---|
| BGM 6곡 | Streaming / Vorbis | 원본 PCM이 통째로 빌드에 들어가던 것 차단 |
| 단발 효과음 | DecompressOnLoad | 스트리밍하면 **눌린 뒤에** 소리가 난다 |
| 도트 스프라이트 | Point · 무압축 · 밉맵 끔 | 이중선형 필터로 흐려지는 것 방지 |

**1인 개발에 특히 필요했던 안정성**
**널 세이프 슬롯** — 클립·스프라이트가 비어도 안 멈춘다(*1인 개발은 항상 반쯤 배선된 상태다*) ·
**도메인 리로드 OFF 대응** `ResetStatics()` · 세이브 **v1→v2→v3** 마이그레이션 ·
정적 서비스 19종이 `static class`라 **`DontDestroyOnLoad`가 단 2개**

---

# 13장 — Future Work: 정직하게 남은 것

> **부제**: 시스템은 완성했고, 콘텐츠와 검증이 남았다.

| 항목 | 상태 |
|---|---|
| **튜토리얼 2종** | 설계·구현 완료 — 검증·영상 녹화 전이라 **현재 비활성화** |
| 효과음 3종 | 슬롯 배선 완료, 클립 선정만 남음 |
| 잡몹 밸런스 수치 | 시스템 완료, 수치 입력 전 (공용값으로 동작 중) |
| 콘텐츠 볼륨 | 월드 2개(6판). 시스템은 확장 대비 완료 |

**남은 것이 적은 이유**
전부 **데이터를 채우는 일**이고 코드를 고칠 일이 아니다.
스테이지·캐릭터·언어·기믹 어느 쪽으로 늘려도 **에셋 1개 또는 상속 1개**로 붙는다.

> 발표 팁: 튜토리얼이 꺼져 있다는 사실을 **먼저** 말할 것. 데모 중에 들통나는 것보다 낫다.

---

# 14장 — 회고: 가장 크게 배운 것 3가지

**① 아키텍처는 약속이 아니라 강제여야 한다**
"Core에 Unity를 쓰지 말자"는 지켜지지 않는다. `noEngineReferences: true` 한 줄이
그것을 **컴파일 에러**로 만든 순간부터 규칙이 진짜가 됐다. → 그 덕에 **10장의 반례**를 찾았다.

**② 같은 숫자를 두 곳에 적으면 반드시 어긋난다**
패링 띠(로직 vs 뷰) · 튜토리얼의 밝은 칸과 누를 수 있는 칸 · 지도의 길 두께와 노드 크기 —
전부 같은 원인이었다. **한 곳에서 파생시키면 어긋날 수가 없다.**

**③ 기획 의도를 코드가 배신하는 지점을 찾는 것이 진짜 일**
잡몹 위협을 **턴**으로 세었더니 *가만히 있는 것이 최적해*가 됐다 → 실시간으로 변경.
소울을 **클리어한 판에서 0**으로 했더니 *빨리 깬 사람이 손해*를 봤다 → 스테이지별 매장량으로.

> **돌아가게 만드는 것과 「의도대로 돌아가게」 만드는 것은 다른 일이었다.**

---

## 부록 — 넣을 자리를 잡아 둘 데모 화면

정지 이미지만으로는 설득이 약한 것들. **직접 촬영이 필요하다.**

| 무엇 | 어디에 | 왜 |
|---|---|---|
| 패링 판정 링 (5초) | 5장 | **"보이는 것 = 판정"**은 움직여야 설명된다 |
| 퍼즐 낙하·연쇄 (5초) | 6장 | 대각 슬라이드·캐스케이드가 한눈에 보인다 |
| 채보 에디터 ▶ 미리듣기 (5초) | 7 또는 8장 | **툴이 실제로 도는 것**을 보여야 툴링 주장이 산다 |
| 인살 컷씬 (5초) | 5장 | 연출 완성도 |

> ⚠ 튜토리얼은 현재 비활성이라 촬영 대상에서 제외.
